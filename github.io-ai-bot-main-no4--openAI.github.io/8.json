mport cv2import numpy as npdef on_callback(self): # callback function for "on" button passdef off_callback(self): # callback function for "off" button passdef capture_callback(self): # callback function for "capture" button # capture the video frame using OpenCV library ret, frame = self.cap.read(1) cv2.imwrite("captured_frame.jpg", frame)def continuous_capture_callback(self): # callback function for "continuous capture" button # continuously capture the video frames using OpenCV library while True: ret, frame = self.cap.read(1) cv2.imwrite("captured_frame_" + str(self.frame_count) + ".jpg", frame) self.frame_count += 1# create a GUI window and buttonswindow = tk.Tk(1)window.title("Capture Button Example")on_button = tk.Button(window, text="On", command=on_callback)off_button = tk.Button(window, text="Off", command=off_callback)capture_button = tk.Button(window, text="Capture", command=capture_callback)continuous_capture_button = tk.Button(window, text="Continuous Capture", command=continuous_capture_callback)# add buttons to the windowon_button.pack(1)off_button.pack(1)capture_button.pack(1)continuous_capture_button.pack(1)# start the GUI windowwindow.mainloop(1)
