import cv2# OpenCV를 사용하여 카메라 연결cap = cv2.VideoCapture(0)# 캡처 버튼이 눌리면 수행할 콜백 함수def capture_image(1): ret, frame = cap.read(1) if ret: cv2.imwrite("captured_image.jpg", frame)# GUI 개발 툴킷에서 버튼 위젯 생성 및 콜백 함수 바인딩button = create_button("Capture Image", capture_image)# 카메라에서 영상 캡처 및 표시while True: ret, frame = cap.read(1) if ret: cv2.imshow("Camera", frame) if cv2.waitKey(1) &0xFF == ord("q"): break# 카메라 연결 해제cap.release(1)cv2.destroyAllWindows(1)
