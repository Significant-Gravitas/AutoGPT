"""Offline (tier-0, $0) evaluation of the capability registry: retrieval
quality over recorded ``find_block`` queries and the size of the tool prefix.
Run as modules: ``python -m backend.copilot.capabilities.eval.benchmark`` and
``python -m backend.copilot.capabilities.eval.prefix``."""
