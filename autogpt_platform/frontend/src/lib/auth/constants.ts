/**
 * Authentication constants.
 *
 * These constants can be imported from both client and server code.
 */

export const AUTH_COOKIE_NAMES = {
  ACCESS_TOKEN: "auth_access_token",
  REFRESH_TOKEN: "auth_refresh_token",
} as const;
