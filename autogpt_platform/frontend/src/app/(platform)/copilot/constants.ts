/**
 * Minimum credit balance (in cents) required to offer the rate limit reset option.
 * Users with less than this amount are prompted to add credits instead.
 */
export const MIN_CREDITS_FOR_RESET = 500; // $5.00
