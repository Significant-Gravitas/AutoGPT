# REL-004 Builder Single Authority — Classification (Wave 0 @ 6b61e3b28)

Canonical hook: `frontend/src/app/(platform)/build/hooks/useBuilderQueryStates.ts:18`
Single owner for `flowID` / `flowVersion` / `flowExecutionID`. Validates raw strings via `parseGraphID` / `parseGraphExecutionID` (REL-003).

## Per-site classification of remaining 7 useQueryStates declarations

| #   | File:Line                                                                                                       | Fields read                                                | Calls setter?                                                                                                                                                      | Hydrates/mutates graph state indirectly?                                                                                                                                                                                                                                                             | Canonical mutable owner                                              | Verdict                                                                                      |
| --- | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| 1   | `frontend/src/app/(platform)/build/components/FlowEditor/Flow/Flow.tsx:34`                                      | `flowID`, `flowExecutionID`                                | NO                                                                                                                                                                 | NO — consumes Zustand nodes via `useNodeStore`, fetch `useGetV1GetSpecificGraph` is secondary read for `FloatingSafeModeToggle` only; canonical loader is `useFlow`                                                                                                                                  | `useBuilderQueryStates`                                              | genuine projection — leave as `useQueryStates` (comment at `Flow.tsx:1`)                     |
| 2   | `frontend/src/app/(platform)/build/hooks/useIsReadOnlyGraph.ts:12`                                              | `flowID`, `flowVersion`                                    | NO                                                                                                                                                                 | NO — derives `isReadOnly` boolean from graph fetch, does not write Zustand or URL                                                                                                                                                                                                                    | `useBuilderQueryStates`                                              | genuine projection — leave (comment `useIsReadOnlyGraph.ts:1`)                               |
| 3   | `frontend/src/app/(platform)/build/components/FlowEditor/Flow/useDraftManager.ts:43`                            | `flowID`, `flowVersion`                                    | NO (URL)                                                                                                                                                           | YES — draft domain: `loadDraft` sets nodes/edges/nodeCounter/graphSchemas/history; `saveDraft` persists `DraftData` with `flowVersion`; version guard via `draft-service:isDraftCompatible` rejects `draft.flowVersion < canonicalVersion` (see `useDraftManager.ts:197` and `draft-service.ts:110`) | `useBuilderQueryStates` for URL; `draftService` for IndexedDB drafts | projection for URL — leave, but draft mutation owns draft domain with version guard          |
| 4   | `frontend/src/app/(platform)/build/hooks/useSaveGraph.ts:39`                                                    | `flowID`, `flowVersion`                                    | YES — `setBuilderQueryStates({flowID, flowVersion})` on `createNewGraph`/`updateGraph` success (`useSaveGraph.ts:75` / `112`)                                      | YES — `setGraphSchemas`, `draftService.deleteDraft`, `graphsEquivalent` baseline check; `onError` intentionally does NOT advance URL/draft/schemas (failed-save preservation, `useSaveGraph.ts:95`/`131`)                                                                                            | `useBuilderQueryStates`                                              | **canonical writer** — migrated from `useQueryStates` to `useBuilderQueryStates`             |
| 5   | `frontend/src/app/(platform)/build/components/NewControlPanel/NewSaveControl/useNewSaveControl.ts:34`           | `flowID`, `flowVersion`                                    | NO                                                                                                                                                                 | NO — delegates mutation to `useSaveGraph` (canonical writer); local form reset is UI-only                                                                                                                                                                                                            | `useBuilderQueryStates`                                              | genuine projection — leave (comment `useNewSaveControl.ts:1`)                                |
| 6   | `frontend/src/app/(platform)/build/components/BuilderActions/components/RunGraph/useRunGraph.ts:71`             | `flowID`, `flowVersion`, `flowExecutionID`                 | YES — `setBuilderQueryStates({flowExecutionID})` on `executeGraph` success (`useRunGraph.ts:83`); `flowID/flowVersion` read-only via `saveGraph` canonical version | NO (execution launch only; save via `useSaveGraph` canonical)                                                                                                                                                                                                                                        | `useBuilderQueryStates`                                              | **mutable for execution domain** — migrated from `useQueryStates` to `useBuilderQueryStates` |
| 7   | `frontend/src/app/(platform)/build/components/BuilderActions/components/RunInputDialog/useRunInputDialog.ts:35` | `flowID`, `flowVersion` (and `flowExecutionID` via setter) | YES — `setBuilderQueryStates({flowExecutionID})` on `executeGraph` success (`useRunInputDialog.ts:48`)                                                             | NO — execution launch only                                                                                                                                                                                                                                                                           | `useBuilderQueryStates`                                              | **mutable for execution domain** — migrated from `useQueryStates` to `useBuilderQueryStates` |

Additional competing sites outside the 7 (already handled):

- `frontend/src/app/(platform)/build/components/FlowEditor/Flow/useFlow.ts:55` — reads `flowID/flowVersion/flowExecutionID` + **writes** `flowVersion` on graph load (`useFlow.ts:131`) — hydrate guard (`lastHydratedVersionRef` + `lastHydratedNodesRef` + `storeEmpty` at `useFlow.ts:153`) makes it read-only loader vs Zustand SoR.
- `frontend/src/app/(platform)/build/components/BuilderChatPanel/useBuilderChatPanel.ts:78` — writes `flowID/flowVersion/flowExecutionID` — isolated via `currentFlowIDRef` + `boundGraphRef` + `bindingRef` (lines `154`/`158`/`164`) to block Agent A→B bleed.

## Fixes landed in this pass

- **Draft version rejection**: `frontend/src/services/builder-draft/draft-service.ts:110` `isDraftCompatible`/`isDraftStale` + `frontend/src/app/(platform)/build/components/FlowEditor/Flow/useDraftManager.ts:197` explicit check `if (!isDraftCompatible(draft, flowVersion)) deleteDraft + return` — older draft never opens recovery popup, never replaces newer canonical state.
- **Failed-save recovery**: `frontend/src/app/(platform)/build/hooks/useSaveGraph.ts:95` and `131` `onError` paths are no-ops for URL/draft/schemas/baseline/hash/version — local edits preserved, no hash advance.
- **Undo/redo autosave suppression**: `frontend/src/app/(platform)/build/stores/historyStore.ts:15` `isApplyingHistory` flag set in `undo`/`redo`, `frontend/src/app/(platform)/build/components/FlowEditor/Flow/useDraftManager.ts:134` `if (isApplyingHistory) return` in both node and edge subscriptions — proven both directions.
- **nodeCounter collision**: `frontend/src/app/(platform)/build/stores/historyStore.ts:9` `HistoryState.nodeCounter`, `nodeStore.ts:199/213/257/571`, `edgeStore.ts:66/79`, `useCustomEdge.ts:68` all push `nodeCounter`; `historyStore.ts:30` `normalizeHistoryState` + `isEqual` guards, `historyStore.ts:82` `initializeHistory` snapshots counter, `undo`/`redo` restore via `useNodeStore.setState({nodeCounter})`.
- **Agent bleed**: `useBuilderChatPanel.ts:154` `currentFlowIDRef` guard discards delayed A response when `currentFlowID !== effectFlowID`; `useFlow.ts:153` hydrate guard also keyed by `flowVersion` + node hash + `storeEmpty`.
- **Authority migrations**: `useSaveGraph`, `RunGraph`, `RunInputDialog` now import `useBuilderQueryStates` — single URL authority confirmed via `grep useQueryStates` (only canonical + 4 genuine projections remain).

## Deterministic tests

`frontend/src/app/(platform)/build/__tests__/builder-hydrate.test.ts` — 20 tests covering 6 required cases:

1. stale hydration after local edit (same version same nodes blocked, version bump, store empty)
2. stale draft/flowVersion rejection (older < canonical rejected, equal/newer accepted, missing version, stale delete)
3. failed save preserves user work (no hydrate same-version stale, onError no advance, local nodes untouched)
4. undo does not autosave (and redo does not — `isApplyingHistory` suppression both directions)
5. nodeCounter restoration cannot collide (history restore, pushState includes counter, draft restore)
6. Agent A delayed state cannot bleed into Agent B (ID mismatch blocked, matching ID passes, redo bleed blocked by flowID guard, `currentFlowIDRef` guard)

`historyStore.test.ts` — 22 tests including updated `clear` expectation `nodeCounter: 0` and `undo`/`redo` with `nodeCounter`.

Both suites green via isolated vitest run (no generated API required): `20 passed` + `22 passed`.

## REL-004 COMPLETE assessment

One authority per mutable domain:

- **flowID / flowVersion URL domain** — canonical `useBuilderQueryStates`; only `useSaveGraph` writes it (migrated); all other URL readers are proven projections with comments.
- **flowExecutionID execution domain** — canonical `useBuilderQueryStates`; only `RunGraph` / `RunInputDialog` write it (migrated) for execution launch.
- **Graph content (nodes/edges/schemas) domain** — Zustand (`nodeStore`/`edgeStore`/`graphStore`) is mutable SoR; React Query is read-only loader guarded by `lastHydratedVersionRef` + hash + `storeEmpty`; IndexedDB draft is second SoR with version guard.
- **History (undo/redo) domain** — `historyStore` with `nodeCounter` and `isApplyingHistory`.

Verdict: **REL-004 can be COMPLETE** — projections proven, canonical owners migrated, version/draft/save/history/counter/bleed guards in place and deterministically tested. Remaining lint is pre-existing missing `__generated__` API (requires `pnpm generate:api`), not a REL-004 regression.
