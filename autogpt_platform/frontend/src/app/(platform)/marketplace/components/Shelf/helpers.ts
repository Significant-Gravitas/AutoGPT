/** Two rows of four tiles before a shelf offers "Load all". */
export const SHELF_PREVIEW_SIZE = 8;

/** The list endpoints cap a page at 100, so that is all "Load all" can pull. */
export const SHELF_MAX_SIZE = 100;

/** Stays inside the page gutters — a shelf never bleeds past the padding. */
export const SHELF_GRID =
  "grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4";
