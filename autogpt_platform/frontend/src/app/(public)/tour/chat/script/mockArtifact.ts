import type { ArtifactRef } from "@/app/(platform)/copilot/store";

export const mockArtifact: ArtifactRef = {
  id: "tour-artifact-1",
  title: "competitor-pricing-watcher.json",
  mimeType: "application/json",
  sourceUrl: "",
  origin: "agent",
  sizeBytes: 4096,
};
