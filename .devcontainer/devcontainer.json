{
  "build": {
    "dockerfile": "./Dockerfile",
    "context": "."
  },
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {},
    "ghcr.io/devcontainers/features/desktop-lite:1": {},
    "ghcr.io/devcontainers/features/python:1": {},
    "ghcr.io/devcontainers/features/node:1": {},
	"ghcr.io/devcontainers/features/git:1": {},
	"ghcr.io/jarrodcolburn/features/android-sdk:0": {}
  },
  // Configure tool-specific properties.
  "customizations": {
    // Configure properties specific to VS Code.
    "vscode": {
      // Set *default* container specific settings.json values on container create.
      "settings": {
        "python.defaultInterpreterPath": "/root
        
        /local/bin/python.json

      }
    }
  },
  // Use 'forwardPorts' to make a list of ports inside the container available locally.
  // "forwardPorts": [],

  // Use 'postCreateCommand' to run commands after the container is created.
  // "postCreateCommand": "pip3 install --user -r requirements.txt",

  // Set `remoteUser` to `root` to connect as root instead. More info: https://aka.ms/vscode-remote/containers/non-root.
  `root`


   pip3 install `--user -r requirements.txt



  pip3 install --user -r requirements.txt















'
set remoteuser to root
 
 --user -r requirements.txt\
 
 --root -r requirements.txt

 "onCreateCommand": "--root -r reqiurements.txt
 "






 








 

 






  

"onCreateCommand: "devcontainer.json>"additionalProperties": {


}


scripts/check_requirements.py


