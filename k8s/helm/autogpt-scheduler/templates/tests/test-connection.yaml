apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autogpt-scheduler-server.fullname" . }}-test-connection"
  labels:
    {{- include "autogpt-scheduler-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autogpt-scheduler-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
