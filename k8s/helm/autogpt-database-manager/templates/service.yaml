apiVersion: v1
kind: Service
metadata:
  name: {{ include "autogpt-database-manager.fullname" . }}
  labels:
    {{- include "autogpt-database-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autogpt-database-manager.selectorLabels" . | nindent 4 }}