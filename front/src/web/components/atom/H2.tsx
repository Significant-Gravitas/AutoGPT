import colored from "@/style/colored"
import styled from "styled-components"

const H2 = styled.h2`
  margin: 0;
  font-size: 1.75rem;
  font-weight: 500;
  line-height: 1.2;
  color: var(--text-color);
`

export default colored(H2)
