apiVersion: v1
kind: Service
metadata:
  name: {{ include "autogpt_server.fullname" . }}
  labels:
    {{- include "autogpt_server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autogpt_server.selectorLabels" . | nindent 4 }}
